#pragma once

namespace tactics {

/** Default seat count for demos (master CLI, Unreal `ResetDemoMatch`, editor console). Turn order is P1..Pn. */
inline constexpr int kDefaultDemoSeatCount = 2;

}  // namespace tactics
